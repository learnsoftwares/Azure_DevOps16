Prerequestie
        https://code.visualstudio.com/

GIT tools

    Installation

        https://github.com/git-for-windows/git/releases/download/v2.37.3.windows.1/Git-2.37.3-64-bit.exe

       Configure

        git config --list
        git config --global user.name "anyname"
        git config --global user.email "anymailid@gmail.com"

        Purpose:

            Developers - share their code (managing the code among the developers)

Github (git providers)
        github
        gitlab
        bitbucket

        Repositories - Private/public


Git commands

    git clone  - clone the repostiory to your local

    git add filename - add to git tracking
            git add filename filename
            git add .

    git rm --cached filename - remove tracking

    git commit -m singleword
            git commit -m "Its in sentence"
    
    git push - Push the changes to remote whatever in committed state 

    git pull - pull the changes from remote to local



Help commands

    git status
    git --version
    git -h
    



